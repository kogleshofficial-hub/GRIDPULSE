import { sql } from "@/lib/db";

type Summary={reports:number;validated:number;regions:number;lastReport:string|null};
async function getSummary():Promise<Summary>{try{const r=await sql<Summary>(`SELECT (SELECT count(*)::int FROM gridpulse.telemetry_reports) reports,(SELECT count(*)::int FROM gridpulse.validation_metrics WHERE validation_status=$1) validated,(SELECT count(*)::int FROM gridpulse.region_clusters) regions,(SELECT max(observed_at)::text FROM gridpulse.telemetry_reports) "lastReport"`,["validated"]);return r.rows[0]??{reports:0,validated:0,regions:0,lastReport:null}}catch{return {reports:0,validated:0,regions:0,lastReport:null}}}
export default async function Home(){const s=await getSummary();return <div className="shell"><header className="top"><div className="brand">GRIDPULSE // CONTROL</div><div className="live">● SYSTEM ONLINE</div></header><main className="main"><section className="grid"><div className="panel"><div className="label">TELEMETRY REPORTS</div><div className="value">{s.reports.toLocaleString()}</div></div><div className="panel"><div className="label">VALIDATED EVENTS</div><div className="value ok">{s.validated.toLocaleString()}</div></div><div className="panel"><div className="label">REGION CLUSTERS</div><div className="value">{s.regions.toLocaleString()}</div></div></section><section className="panel map"><span className="node" style={{left:"22%",top:"34%"}}/><span className="node" style={{left:"61%",top:"48%"}}/><span className="node" style={{left:"78%",top:"28%"}}/></section><section className="panel"><div className="label">NETWORK STATUS</div><table><thead><tr><th>METRIC</th><th>VALUE</th><th>STATE</th></tr></thead><tbody><tr><td>LAST OBSERVED REPORT</td><td>{s.lastReport??"—"}</td><td className="ok">{s.lastReport?"INGESTED":"NO DATA"}</td></tr><tr><td>VALIDATION ENGINE</td><td>POSTGRESQL</td><td className="ok">READY</td></tr><tr><td>AI PIPELINE</td><td>AZURE</td><td className="warn">AWAITING TELEMETRY</td></tr></tbody></table></section></main><footer className="footer">GRIDPULSE // NO FABRICATED LIVE TELEMETRY // UTC DATA MODEL</footer></div>}
